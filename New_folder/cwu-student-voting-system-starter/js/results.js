document.addEventListener("DOMContentLoaded",()=>{const resultList=document.querySelector("#resultList");if(resultList){resultList.innerHTML=DEMO_RESULTS.map(r=>`<div class="result-row"><img src="${r.image}" alt="${r.name}"><div><strong>${r.name}</strong><div class="result-bar"><span style="--w:${r.percent}%"></span></div><small style="color: var(--muted);">${r.votes} votes</small></div><strong style="font-size:1.4rem;">${r.percent}%</strong></div>`).join("")}const canvas=document.getElementById("resultsChart");if(canvas&&window.Chart){new Chart(canvas,{type:"doughnut",data:{labels:DEMO_RESULTS.map(r=>r.name),datasets:[{data:DEMO_RESULTS.map(r=>r.votes),borderWidth:0}]},options:{plugins:{legend:{position:"bottom"}}}})}});